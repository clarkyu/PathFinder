## 关联

- 关联 Issue：#

## 变更摘要

-

## 循环工程检查单（docs/loop.md）

- [ ] `npm test` 本地/CI 全部通过
- [ ] 最小改动：未夹带与 issue 无关的修改
- [ ] 版本号已按规则更新（`js/data.js` 的 `version` 与 `sw.js` 的 `CACHE` 同步）
- [ ] `docs/design.md` 迭代记录已追加一行
- [ ] 未触碰红线（隐私承诺 / 本地存储原则 / 零运行时依赖 / 数据消毒）
